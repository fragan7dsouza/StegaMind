# StegaMind
StegaMind is an AI-powered steganography and steganalysis system built with PyTorch, FastAPI, and a modern React + Tailwind UI. It hides secret images inside cover images, detects stego content, and extracts hidden data with a trained autoencoder and CNN classifier.
